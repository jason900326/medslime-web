# Supabase
NEXT_PUBLIC_SUPABASE_URL=your-project-url
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your-publishable-or-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-server-only-service-role-key

# Canonical public site URL used for payment callbacks
NEXT_PUBLIC_SITE_URL=https://medslime.vercel.app

# ECPay AioCheckOut
# Keep HashKey / HashIV server-side only. Never expose them with NEXT_PUBLIC_.
ECPAY_MERCHANT_ID=your-merchant-id
ECPAY_HASH_KEY=your-hash-key
ECPAY_HASH_IV=your-hash-iv
# stage | production
ECPAY_MODE=stage
# Enable the visible checkout buttons only after the server vars and SQL are ready.
NEXT_PUBLIC_ECPAY_ENABLED=false
